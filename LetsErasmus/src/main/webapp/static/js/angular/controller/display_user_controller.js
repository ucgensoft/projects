App.controller('displayUserCtrl', ['$scope', '$controller', 'placeService', 
                                function($scope, $controller, placeService) {
      var self = this;
      
      self.initialize = function() {
    	  
	 };
  	
    self.initialize();
      
  }]);